import CodeBlock from "../../_components/docs/CodeBlock";
import { Callout } from "../_components/Callout";

export const metadata = {
	title: "AnimateIcons MCP Server",
	description:
		"Connect the @animateicons/mcp Model Context Protocol server so AI coding agents like Claude Code and Cursor can search and add animated React icons for you.",
	alternates: {
		canonical: "/icons/docs/mcp",
	},
};

export const mcpClaudeCode = `claude mcp add animateicons -- npx -y @animateicons/mcp`;

export const mcpJsonCode = `{
	"mcpServers": {
		"animateicons": {
			"command": "npx",
			"args": ["-y", "@animateicons/mcp"]
		}
	}
}`;

export const mcpUsageCode = `// Then just ask your agent in plain language:
//   "add a notification bell icon"
//
// → it calls search_icons, picks bell-ring, then add_icon
//   and writes components/icons/bell-ring.tsx for you.`;

# MCP server

`@animateicons/mcp` is a [Model Context Protocol](https://modelcontextprotocol.io) server. Connect it to an AI coding agent (Claude Code, Cursor, and others), and the agent can search the AnimateIcons catalog and drop animated icons straight into your project, no copy-pasting.

<div className="space-y-14">

<section id="tools" className="scroll-mt-20 space-y-4">

## What it can do

Once connected, your agent gets four tools:

- `search_icons`: fuzzy-search the catalog by name, keyword, or category.
- `get_icon`: return an icon's component source plus a ready-to-paste import (read-only).
- `add_icon`: write an icon's component file into your project (default `components/icons/`).
- `list_libraries`: list the libraries and how many icons each has.

</section>

<section id="claude-code" className="scroll-mt-20 space-y-6">

## Claude Code

Register the server in one command:

<CodeBlock code={mcpClaudeCode} lang="bash" title="Terminal" />

<Callout type="tip">
Restart Claude Code (or reload your editor window) after adding the server so it registers the new tools.
</Callout>

Then just ask, and the agent picks the right tool:

<CodeBlock code={mcpUsageCode} lang="tsx" title="In your editor" />

</section>

<section id="other-clients" className="scroll-mt-20 space-y-6">

## Cursor & other clients

Add the server to your MCP config (e.g. `.cursor/mcp.json` or `.mcp.json`):

<CodeBlock code={mcpJsonCode} lang="json" title=".mcp.json" />

</section>

<section id="requirements" className="scroll-mt-20 space-y-4">

## Requirements

<Callout type="note">
Icons written by `add_icon` import `cn` from `@/lib/utils` (the shadcn convention) and require the [motion](https://motion.dev) package at runtime.
</Callout>

Prefer the terminal? The [CLI](/icons/docs/cli) does the same thing with `npx animateicons add`.

</section>

</div>
