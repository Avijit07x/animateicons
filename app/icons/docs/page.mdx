import Link from "next/link";
import CodeBlock from "../_components/docs/CodeBlock";
import CommandBlock from "../_components/docs/CommandBlock";
import { Callout } from "./_components/Callout";
import InstallMethods from "./_components/InstallMethods";
import { ICON_COUNTS } from "@/lib/icon-count.generated";

export const metadata = {
	title: "Install AnimateIcons",
	description:
		"Install AnimateIcons via the @animateicons/react npm package, the animateicons CLI, the shadcn CLI, or the MCP server for AI agents.",
	alternates: {
		canonical: "/icons/docs",
	},
};

export const npmUsageCode = `import { EyeIcon } from "@animateicons/react/lucide"

export default function Example() {
	return <EyeIcon size={24} color="#f45b48" />
}`;

# Installation

Pick whichever path fits your setup. They all ship the same icons and the same API. The difference is whether you depend on the package or copy the source into your project.

<InstallMethods />

<div className="space-y-14">

<section id="install-npm" className="scroll-mt-20 space-y-6">

## npm package

<span className="border-emerald-500/30 bg-emerald-500/10 text-emerald-400 mr-2 inline-block rounded-full border px-2.5 py-0.5 align-middle text-xs font-semibold">Recommended</span> Best for most apps. One install, all {ICON_COUNTS.total} icons available.

#### 1. Install the package

Use any package manager. `motion` is bundled.

<CommandBlock pkg="@animateicons/react" />

#### 2. Import an icon

Lucide and Huge are exposed as scoped subpaths because some icon names overlap (`HeartIcon`, `CopyIcon`, etc.).

<CodeBlock code={npmUsageCode} title="Example.tsx" />

<Callout type="tip">
Icons animate on hover out of the box, no extra wiring. Pass `size`, `color`, or any prop to customize.
</Callout>

</section>

<section id="other-methods" className="scroll-mt-20 space-y-4">

## Prefer to copy the source?

If you'd rather own the icon files instead of depending on the package, use the <Link href="/icons/docs/shadcn">shadcn CLI</Link> or the first-party <Link href="/icons/docs/cli">animateicons CLI</Link>. Using an AI coding agent? The <Link href="/icons/docs/mcp">MCP server</Link> lets it add icons for you.

Once icons are in your project, head to <Link href="/icons/docs/usage">Usage</Link> for styling, the imperative API, and full types.

</section>

</div>
