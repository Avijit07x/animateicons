import Link from "next/link";
import BackButton from "../_components/docs/BackButton";
import CodeBlock from "../_components/docs/CodeBlock";
import OpenInAI from "../_components/docs/OpenInAI";

export const metadata = {
	title: "Install AnimateIcons",
	description:
		"Install AnimateIcons via the @animateicons/react npm package or the shadcn CLI.",
	alternates: {
		canonical: "/icons/docs",
	},
};

export const npmInstallCode = `pnpm add @animateicons/react`;

export const npmUsageCode = `import { EyeIcon } from "@animateicons/react/lucide"

export default function Example() {
	return <EyeIcon size={24} color="#f45b48" />
}`;

export const installCode = `pnpm dlx shadcn@latest add https://animateicons.in/r/lu-eye.json`;

export const usageCode = `import { EyeIcon } from "@/components/ui/eye"

export default function Example() {
	return <EyeIcon size={24} color="#f45b48" />
}`;

export const stylingCode = `// Color — sets currentColor inline
<EyeIcon color="#f45b48" />

// Tailwind utility — works because icons stroke="currentColor"

<EyeIcon className="text-primary" />

// Speed — duration is a multiplier (lower = faster)

<EyeIcon duration={0.6} />

// Disable hover animation

<EyeIcon isAnimated={false} />
`;

export const apiCode = `"use client"

import { useRef } from "react"
import { EyeIcon, type EyeIconHandle } from "@/components/ui/eye"

export default function Demo() {
	const ref = useRef<EyeIconHandle>(null)

    return (
    	<button
    		onMouseEnter={() => ref.current?.startAnimation()}
    		onMouseLeave={() => ref.current?.stopAnimation()}
    	>
    		<EyeIcon ref={ref} size={28} />
    	</button>
    )

}`;

export const propsCode = `interface IconProps {
	size?: number
	color?: string
	className?: string
	duration?: number
	isAnimated?: boolean
	onMouseEnter?: (e: React.MouseEvent<HTMLDivElement>) => void
	onMouseLeave?: (e: React.MouseEvent<HTMLDivElement>) => void
	style?: React.CSSProperties
}

interface IconHandle {
startAnimation: () => void
stopAnimation: () => void
}`;

<div className="min-w-0 flex-1">
	<div className="text-textPrimary mx-auto w-full max-w-5xl px-6 py-12">
		<div className="mb-10 flex w-full flex-wrap items-center justify-between gap-5">
			<div className="flex items-center gap-3">
				<BackButton />
				<h1 className="text-2xl font-semibold">Installation</h1>
			</div>
			<OpenInAI
				pageUrl="https://animateicons.in/icons/docs"
				title="AnimateIcons Documentation"
			/>
		</div>

Pick one of the two paths below. Both ship the same icons and the same API — the only difference is whether you depend on the package or own the source.

<div className="space-y-14">

<section id="install-npm" className="scroll-mt-20 space-y-6">

## npm package

Recommended for most apps. One install, all 281 icons available.

#### 1. Install the package

Use any package manager — npm, pnpm, yarn, or bun. `motion` is bundled.

<CodeBlock code={npmInstallCode} lang="bash" title="Terminal" />

#### 2. Import an icon

Lucide and Huge are exposed as scoped subpaths because some icon names overlap (`HeartIcon`, `CopyIcon`, etc.).

<CodeBlock code={npmUsageCode} title="Example.tsx" />

That's it — the icon animates on hover by default.

</section>

<section id="install-shadcn" className="scroll-mt-20 space-y-6">

## shadcn CLI

Use this if you want each icon copied into your codebase as a single file you can edit.

#### 1. Set up shadcn

If your project doesn't have shadcn yet, follow the <Link href="https://ui.shadcn.com/docs/installation" target="_blank" className="underline underline-offset-4">shadcn installation guide</Link> first.

#### 2. Add an icon

Browse the <Link href="/icons/lucide" className="underline underline-offset-4">Lucide</Link> or <Link href="/icons/huge" className="underline underline-offset-4">Huge</Link> gallery, click any tile to copy its install command, or replace `lu-eye` below with the icon you want.

<CodeBlock code={installCode} lang="bash" title="Terminal" />

The icon lands at `components/ui/<name>.tsx`.

#### 3. Import and use

<CodeBlock code={usageCode} title="Example.tsx" />

</section>

<section id="styling" className="scroll-mt-20 space-y-4">

## Styling

Every icon strokes `currentColor`, so it inherits the surrounding text color. You can also pass `color`, `className`, or use the `duration` and `isAnimated` props to control playback.

<CodeBlock code={stylingCode} title="Styling.tsx" />

</section>

<section id="api" className="scroll-mt-20 space-y-4">

## Imperative API

Need to trigger an animation from a parent — on click, on focus, or programmatically? Pass a ref. Each icon exports its own `*Handle` type.

<CodeBlock code={apiCode} title="Demo.tsx" />

</section>

<section id="props" className="scroll-mt-20 space-y-4">

## Props & types

<CodeBlock code={propsCode} lang="ts" title="Types" />

Animations respect the OS-level **Reduce Motion** preference — no extra setup required.

</section>

</div>
	</div>
</div>
